# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# ==================== EMBEDDING PROVIDER ====================
# Choose embedding provider: 'ollama' or 'azure'
# Default: ollama (free, local)
EMBEDDING_PROVIDER=ollama

# ==================== CHROMADB CONFIGURATION ====================
CHROMA_DB_PATH=./harry-rag-chroma-db

# ==================== OLLAMA CONFIGURATION ====================
# Used when EMBEDDING_PROVIDER=ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=nomic-embed-text:latest
OLLAMA_LLM_MODEL=gemma3:1b

# ==================== MCP CONFIGURATION ====================
# Model Context Protocol server settings
MCP_SERVER_NAME=NexQA MCP
MCP_TRANSPORT=stdio
MCP_HOST=127.0.0.1
MCP_PORT=8001
MCP_DEFAULT_USER_ID=harry

# ==================== AZURE OPENAI CONFIGURATION ====================
# Required when EMBEDDING_PROVIDER=azure or when using Azure for LLM calls

# Azure OpenAI API Key
AZURE_OPENAI_API_KEY=

# Azure OpenAI Endpoint
AZURE_OPENAI_ENDPOINT=

# Azure OpenAI API Version (optional, default: 2024-02-01)
AZURE_OPENAI_API_VERSION=2024-02-01

# Azure OpenAI Embedding Model deployment name (optional, default: text-embedding-ada-002)
AZURE_EMBEDDING_MODEL=text-embedding-ada-002

# Azure OpenAI Chat Model deployment name (optional, default: gpt-4)
AZURE_OPENAI_MODEL=gpt-4

# ==================== MIGRATION NOTES ====================
# To switch from Ollama to Azure OpenAI:
# 1. Set EMBEDDING_PROVIDER=azure
# 2. Add your Azure credentials above
# 3. Delete the harry-rag-chroma-db folder (embeddings are not compatible)
# 4. Restart the server
# 5. Re-upload your documents
# See AZURE_MIGRATION.md for detailed instructions
